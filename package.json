{
  "name": "falcon-core-v2",
  "version": "1.0.0",
  "description": "Backend modular de Falcon Core v2 con Firestore",
  "main": "src/core.js",
  "type": "module",
  "scripts": {
    "start": "node src/core.js",
    "firebase:deploy:rules": "firebase deploy --only firestore:rules",
    "deploy:ahau": "npm run build --workspace=frontends/ahau && firebase deploy --only hosting && firebase deploy --only functions:api"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "firebase-admin": "^13.4.0",
    "node-fetch": "^3.3.2"
  }
}
