{
  "name": "ahau",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "export": "next build",
    "deploy": "npm run export && firebase deploy --only hosting:uaylabs"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0"
  }
}
